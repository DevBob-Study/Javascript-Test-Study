function solution(s) {
  let words = s.split(" ");
  let answer = words.map((word) => {
    if (word.length == 0) {
      return "";
    } else {
      return word[0].toUpperCase() + word.slice(1).toLowerCase();
    }
  });
  return answer.join(" ");
}
